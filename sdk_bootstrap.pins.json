{
  "$schema_comment": "Language-repo bootstrap pins (HOW_TO G0). Never invent production COMPAT triples or Hub stamps.",
  "doc": "docs/V2/IMPLEMENTATION_GUIDE.md",
  "pins_version": "1",
  "last_reviewed": "2026-08-11",

  "language": {
    "id": "python",
    "package_name": "kotenai-zeus-client",
    "module_path": "zeus_client",
    "min_runtime": "3.11",
    "greenfield": false
  },

  "claim": {
    "client_floor": "client-floor-5",
    "claim_level": "candidate",
    "comment": "candidate|partial|supported — never 'supported' until suite required cases green",
    "modes": ["agent", "direct"],
    "multi_agent": "no",
    "plugins": "no"
  },

  "suite": {
    "suite_version": "conformance-0.2-dev",
    "design_repo_ref": "local:../zeus_client_design",
    "required_levels": ["L0", "L1", "L2"],
    "detective_tapes_min": ["smooth_short", "fail_zeus"]
  },

  "catalog": {
    "offline_mock_path": "../zeus_client_design/fixtures/catalogs/mock_base5_minimal",
    "mock_base_id": "base-5-mock",
    "production_base_id": null,
    "comment": "Use mock for offline R1. Set production_base_id only from chat_request COMPAT + Hub stamp."
  },

  "zeus": {
    "engine_semver_range": "0.6.x",
    "openapi_path": "../Zeus/docs/openapi.yaml",
    "openapi_git_ref": null,
    "default_base_url": "http://127.0.0.1:8080",
    "live_smoke": false
  },

  "llm": {
    "provider": "xai",
    "api_style": null,
    "base_url": "https://api.x.ai/v1",
    "model": "grok-4-1-non-reasoning",
    "api_key_env": "XAI_API_KEY",
    "context_window_tokens": 128000,
    "context_soft_limit": 0.8,
    "ai_process_result_default": false,
    "comment": "Pins product guidance default false (cheap). Package Runtime ClientSettings.ai_process_result default remains True (Hub parity) — set false explicitly for cheap products."
  },

  "auth_smoke": {
    "mode": "none",
    "comment": "Lab only. Production: bearer|session|basic per deploy."
  },

  "stamps": {
    "user": "zeus_client",
    "version_from": "package_semver"
  },

  "blocked_without": [
    "zeus_client_design (this pin file + HOW_TO + suite)",
    "For floor-5 control plane content: zeus_chat_request clone (often under Zeus/ai/zeus_chat_request)",
    "For live smoke: Zeus running + credentials",
    "For supported claim: suite green + COMPAT row"
  ]
}
