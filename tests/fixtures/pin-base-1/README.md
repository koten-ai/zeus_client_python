# Offline pin-era catalog shape (`base-1`)

**`MOCK_NOT_FOR_PRODUCTION`**

Used only to lock the CHECKLIST A pin-path load/bind tests.  
Not a chat_request COMPAT triple. Not a Hub-published stamp.

| File | Role |
| --- | --- |
| `chat_request_analytics_base-1.json` | Lineage `base-1` + local `compute_contract_hash` stamp |
| `response_output_schema.json` | Required-four subset |
